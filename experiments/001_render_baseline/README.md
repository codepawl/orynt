# 001 Render Baseline

Goal: render a small static HTML page locally and collect the first screenshot, DOM, accessibility, and metric artifacts.

Expected future command:

```bash
uv run codepawl-render examples/simple_landing.html --out artifacts/render_baseline
```
