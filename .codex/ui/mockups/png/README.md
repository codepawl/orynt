# PNG previews

These are static raster previews generated for quick visual reference when a file browser cannot render the HTML mockups. The HTML mockups remain the source of truth for layout/components.
