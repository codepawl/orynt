# CodePawl Design

Placeholder for the CodePawl Design product application.

The first implementation priority is not a full frontend app. Build the local render/evaluation harness first so product decisions can be evaluated against real screenshots, DOM data, accessibility data, and metrics.
