{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main CodePawl app window. Native folder picking is limited to explicit repository selection.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "core:window:default", "core:app:default", "dialog:allow-open"]
}
