{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "onboarding",
  "description": "Onboarding setup surface for local alpha settings, local directory picking, and provider checks.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "core:app:default", "dialog:allow-open"]
}
