{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "onboarding",
  "description": "Onboarding setup surface for local alpha settings, repository folder picking, and provider checks.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "core:window:default", "core:app:default", "dialog:allow-open"]
}
