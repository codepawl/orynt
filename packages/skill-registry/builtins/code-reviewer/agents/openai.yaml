interface:
  display_name: "Code Reviewer"
  short_description: "Review repository changes for correctness and risk."
  default_prompt: "Use $code-reviewer to review the current diff and report prioritized findings."
policy:
  allow_implicit_invocation: false
