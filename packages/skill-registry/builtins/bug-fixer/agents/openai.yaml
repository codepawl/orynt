interface:
  display_name: "Bug Fixer"
  short_description: "Reproduce and fix bugs with focused regression tests."
  default_prompt: "Use $bug-fixer to reproduce and fix this bug with a focused regression test."
policy:
  allow_implicit_invocation: false
