interface:
  display_name: "Change Planner"
  short_description: "Plan a bounded repository change from live evidence."
  default_prompt: "Use $change-planner to prepare a decision-complete plan for this repository change."
policy:
  allow_implicit_invocation: false
