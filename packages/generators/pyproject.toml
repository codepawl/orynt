[project]
name = "codepawl-generators"
version = "0.1.0"
description = "Fixture and prompt generators for CodePawl Design research."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
