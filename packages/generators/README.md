# codepawl-generators

Python package for future fixture, prompt, and static HTML generators used by the harness and benchmark.

Keep generators explicit and reproducible so generated examples can be traced back to their parameters.
