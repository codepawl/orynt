[project]
name = "codepawl-jitter"
version = "0.1.0"
description = "Controlled UI perturbation package for Pawl-JEPA data generation."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
