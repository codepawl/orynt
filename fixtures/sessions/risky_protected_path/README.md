# Risky Protected Path Fixture

Expected verdict: `risky`.

Why: protected billing code and dependency files changed. Required validation logs are present, so the report should focus on review risk rather than missing evidence.
