project:
  name: codepawl-fixture

checks:
  test:
    required: true
    log: logs/test.log

policy:
  protected_paths:
    - "schema/**"
  risky_patterns:
    - "Cargo.toml"
  blocked_paths:
    - "schema/destructive/**"
